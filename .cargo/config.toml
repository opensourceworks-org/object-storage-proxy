# [build]
# rustflags = ["-C", "link-arg=-mmacosx-version-min=15.2"]

[env]
# Disable hand-optimised assembly in aws-lc-sys so cross-compilers that don't
# support -march=armv8.4-a+sha3 (e.g. older aarch64-linux-gnu-gcc in CI) don't
# fail the build.
AWS_LC_SYS_NO_ASM = "1"